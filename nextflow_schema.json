{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/alexyfyf/atac_nf/master/nextflow_schema.json",
    "title": "atac_nf pipeline parameters",
    "description": "ATAC-seq analysis Nextflow pipeline",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "schema": "assets/schema_input.json",
                    "description": "Path to a comma-separated file describing the samples.",
                    "help_text": "Requires the columns `sample` and `fastq_1`. `fastq_2` may be empty for single-end samples. Optional `replicate` and `condition` columns are carried through for downstream analysis.",
                    "fa_icon": "fas fa-file-csv"
                },
                "reads": {
                    "type": "string",
                    "description": "Legacy input: a quoted glob matching FASTQ file pairs, e.g. 'data/*_R{1,2}.fq.gz'.",
                    "help_text": "Retained for backwards compatibility with the DSL1 pipeline. Prefer --input, which also carries replicate and condition metadata.",
                    "fa_icon": "fas fa-asterisk"
                },
                "single_end": {
                    "type": "boolean",
                    "default": false,
                    "description": "Treat --reads input as single-end. Ignored when --input is used, where it is decided per sample.",
                    "fa_icon": "fas fa-align-left"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "default": "results",
                    "description": "Output directory for the results.",
                    "fa_icon": "fas fa-folder-open"
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "enum": [
                        "symlink",
                        "rellink",
                        "link",
                        "copy",
                        "copyNoFollow",
                        "move"
                    ],
                    "description": "Method used to save pipeline results to the output directory.",
                    "fa_icon": "fas fa-copy",
                    "hidden": true
                },
                "save_trimmed": {
                    "type": "boolean",
                    "default": false,
                    "description": "Publish the trimmed FASTQ files. They are large and rarely needed.",
                    "fa_icon": "fas fa-save"
                },
                "save_raw_bam": {
                    "type": "boolean",
                    "default": false,
                    "description": "Publish the pre-filtering BAM files.",
                    "fa_icon": "fas fa-save"
                }
            }
        },
        "reference_genome_options": {
            "title": "Reference genome options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Reference genome and derived annotation.",
            "properties": {
                "fasta": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "pattern": "^\\S+\\.(fa|fasta|fna)(\\.gz)?$",
                    "description": "Path to the reference genome FASTA. Required unless --aligner_index or --genome supplies one.",
                    "fa_icon": "fas fa-file"
                },
                "mito_name": {
                    "type": "string",
                    "description": "Mitochondrial contig name. Defaults to the genome preset, or chrM. Excluded from library-complexity metrics and reported separately in the FRiP table.",
                    "fa_icon": "fas fa-dna"
                },
                "aligner": {
                    "type": "string",
                    "default": "bwa",
                    "enum": [
                        "bwa",
                        "bwa-mem2"
                    ],
                    "description": "Read aligner. bwa-mem2 is faster but needs substantially more RAM to build its index.",
                    "fa_icon": "fas fa-map-signs"
                },
                "aligner_index": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Directory containing a pre-built index for the selected aligner. Skips indexing, and makes --fasta optional. Must match --aligner: bwa and bwa-mem2 index formats are not interchangeable.",
                    "fa_icon": "fas fa-bezier-curve"
                },
                "blacklist": {
                    "type": "string",
                    "format": "file-path",
                    "description": "BED file of blacklisted regions. Overrides the genome preset. Must use the same contig naming as the reference (UCSC chr1 vs Ensembl 1) or filtering silently does nothing; the pipeline now checks this at runtime.",
                    "fa_icon": "fas fa-ban"
                },
                "macs_gsize": {
                    "type": "string",
                    "description": "MACS effective genome size ('mm', 'hs', or a number). Overrides the genome preset.",
                    "fa_icon": "fas fa-arrows-alt-h"
                },
                "effective_genome_size": {
                    "type": "string",
                    "description": "Effective genome size used for RPGC normalisation of bigwigs. Overrides the genome preset.",
                    "fa_icon": "fas fa-arrows-alt-h"
                }
            }
        },
        "trimming_options": {
            "title": "Trimming options",
            "type": "object",
            "fa_icon": "fas fa-cut",
            "properties": {
                "trim": {
                    "type": "boolean",
                    "default": true,
                    "description": "Run trimmomatic. When false the reads go straight to alignment.",
                    "fa_icon": "fas fa-cut"
                },
                "adapter": {
                    "type": "string",
                    "default": "atac",
                    "description": "Adapter set: 'atac'/'nextera', 'truseq', or a path to an adapter FASTA.",
                    "fa_icon": "fas fa-file"
                }
            }
        },
        "peak_calling_options": {
            "title": "Peak calling options",
            "type": "object",
            "fa_icon": "fas fa-mountain",
            "properties": {
                "macs_qvalue": {
                    "type": "number",
                    "default": 0.01,
                    "description": "MACS q-value (FDR) cutoff for narrow and broad peak calling.",
                    "fa_icon": "fas fa-filter"
                },
                "macs2qval": {
                    "type": "number",
                    "description": "Deprecated alias for --macs_qvalue.",
                    "fa_icon": "fas fa-history",
                    "hidden": true
                },
                "macs_format": {
                    "type": "string",
                    "default": "BED",
                    "enum": [
                        "BED",
                        "BAMPE"
                    ],
                    "description": "BED treats each read end as an independent Tn5 insertion; BAMPE calls on whole fragments.",
                    "fa_icon": "fas fa-file-code"
                },
                "hmmratac": {
                    "type": "boolean",
                    "default": false,
                    "description": "Additionally call peaks with MACS3 hmmratac.",
                    "fa_icon": "fas fa-mountain"
                }
            }
        },
        "downstream_analysis_options": {
            "title": "Downstream analysis options",
            "type": "object",
            "fa_icon": "fas fa-chart-line",
            "description": "Cross-sample analysis over the consensus peak set.",
            "properties": {
                "gtf": {
                    "type": "string",
                    "format": "file-path",
                    "pattern": "^\\S+\\.gtf(\\.gz)?$",
                    "description": "GTF annotation. Enables TSS enrichment and nearest-gene peak annotation.",
                    "fa_icon": "fas fa-file-alt"
                },
                "tss_window": {
                    "type": "integer",
                    "default": 2000,
                    "description": "Base pairs either side of the TSS in the enrichment profile.",
                    "fa_icon": "fas fa-arrows-alt-h"
                },
                "tss_bin_size": {
                    "type": "integer",
                    "default": 10,
                    "description": "Bin size for the TSS enrichment profile.",
                    "fa_icon": "fas fa-th"
                },
                "deseq2_fdr": {
                    "type": "number",
                    "default": 0.05,
                    "description": "FDR threshold reported for differential accessibility.",
                    "fa_icon": "fas fa-filter"
                },
                "skip_consensus": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip consensus peaks, the counts matrix, DESeq2 and peak annotation.",
                    "fa_icon": "fas fa-forward"
                },
                "skip_deseq2": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip the DESeq2 step only.",
                    "fa_icon": "fas fa-forward"
                },
                "skip_tss_enrichment": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip the TSS enrichment profile only.",
                    "fa_icon": "fas fa-forward"
                },
                "skip_peak_annotation": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip the per-sample ChIPseeker peak annotation.",
                    "help_text": "Needs --gtf, and a GTF with transcript records: a TxDb cannot be built from a gene- or exon-only annotation, unlike the TSS extraction used for TSS enrichment.",
                    "fa_icon": "fas fa-forward"
                },
                "annotation_tss_region": {
                    "type": "integer",
                    "default": 3000,
                    "description": "Half-width in bp of the promoter class in the ChIPseeker annotation.",
                    "help_text": "Separate from --tss_window, which is the range of the deeptools TSS profile rather than a classification threshold. Note also that the two steps use different TSS sets from the same GTF: the profile uses one TSS per gene, ChIPseeker one per transcript.",
                    "fa_icon": "fas fa-arrows-alt-h"
                },
                "exclude_contigs": {
                    "type": "string",
                    "default": "_random$|^chrUn",
                    "description": "Perl regex; alignments on matching contigs are dropped by the Tn5 shift step.",
                    "help_text": "Applied before peak calling, coverage, FRiP and the counts matrix, so excluded contigs are absent from every downstream result. The default reproduces the shift script's historical behaviour and matches UCSC naming only; an Ensembl reference needs its own pattern. Pass an empty string or 'none' to keep every contig. Each sample gets a <sample>.shift_summary.txt recording the pattern and what it removed.",
                    "fa_icon": "fas fa-filter"
                },
                "tss_biotypes": {
                    "type": "string",
                    "default": "all,protein_coding",
                    "description": "Comma-separated gene sets to build TSS-enrichment profiles over.",
                    "help_text": "'all' uses every gene in the GTF; any other value is matched against gene_type (GENCODE) or gene_biotype (Ensembl). Both are produced by default: over all biotypes the profile is diluted by pseudogenes and other low-signal entries, while protein-coding alone is the sharper, ENCODE-comparable metric. A GTF carrying no biotype attributes can only produce the 'all' profile, and says so in the log.",
                    "fa_icon": "fas fa-dna"
                },
                "skip_qc_summary": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip the cross-sample QC summary figure and table.",
                    "fa_icon": "fas fa-forward"
                }
            }
        },
        "track_hub_options": {
            "title": "UCSC track hub options",
            "type": "object",
            "fa_icon": "fas fa-eye",
            "description": "Assemble a UCSC track hub from the per-sample bigWigs and peaks.",
            "properties": {
                "trackhub": {
                    "type": "boolean",
                    "default": false,
                    "description": "Build a UCSC track hub in <outdir>/trackhub.",
                    "help_text": "Requires --fasta (the chromosome sizes come from its index), --trackhub_genome and --trackhub_email.",
                    "fa_icon": "fas fa-eye"
                },
                "trackhub_genome": {
                    "type": "string",
                    "description": "UCSC assembly name the data is aligned to, e.g. hg38 or mm10.",
                    "help_text": "Must be an assembly UCSC hosts. Anything else would need an assembly hub, which this pipeline does not build.",
                    "fa_icon": "fas fa-dna"
                },
                "trackhub_email": {
                    "type": "string",
                    "description": "Contact address written into hub.txt; UCSC requires one.",
                    "fa_icon": "fas fa-envelope"
                },
                "trackhub_name": {
                    "type": "string",
                    "default": "atac_nf",
                    "description": "Hub name. Spaces become underscores; characters other than letters, digits, -, . and _ are stripped.",
                    "fa_icon": "fas fa-tag"
                },
                "trackhub_url": {
                    "type": "string",
                    "description": "Base URL the hub will be served from; only used to print the hub.txt URL at the end of the run.",
                    "fa_icon": "fas fa-link"
                }
            }
        },
        "resource_options": {
            "title": "Resource options",
            "type": "object",
            "fa_icon": "fas fa-microchip",
            "description": "Ceilings applied to every process request.",
            "properties": {
                "max_cpus": {
                    "type": "integer",
                    "description": "Maximum CPUs any single task may request. Defaults to the detected core count.",
                    "fa_icon": "fas fa-microchip"
                },
                "max_memory": {
                    "type": "string",
                    "pattern": "^\\d+(\\.\\d+)?\\s*\\.?\\s*(K|M|G|T)?B$",
                    "default": "128.GB",
                    "description": "Maximum memory any single task may request.",
                    "fa_icon": "fas fa-memory"
                },
                "max_time": {
                    "type": "string",
                    "pattern": "^(\\d+\\.?\\s*(s|m|h|d|day)\\s*)+$",
                    "default": "240.h",
                    "description": "Maximum time any single task may request.",
                    "fa_icon": "far fa-clock"
                },
                "slurm_account": {
                    "type": "string",
                    "description": "SLURM account, passed as --account (used by -profile slurm).",
                    "fa_icon": "fas fa-users"
                },
                "slurm_qos": {
                    "type": "string",
                    "description": "SLURM QOS, passed as --qos (used by -profile slurm).",
                    "fa_icon": "fas fa-users"
                },
                "slurm_queue": {
                    "type": "string",
                    "description": "SLURM partition (used by -profile slurm).",
                    "fa_icon": "fas fa-users"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "properties": {
                "shift": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Perl script used for Tn5 offset correction.",
                    "fa_icon": "fas fa-code",
                    "hidden": true
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/reference_genome_options"
        },
        {
            "$ref": "#/$defs/trimming_options"
        },
        {
            "$ref": "#/$defs/peak_calling_options"
        },
        {
            "$ref": "#/$defs/downstream_analysis_options"
        },
        {
            "$ref": "#/$defs/track_hub_options"
        },
        {
            "$ref": "#/$defs/resource_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ]
}
